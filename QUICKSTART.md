# BarberCRM - Быстрый старт 🚀

## 📦 Что внутри проекта?

```
barber-crm/
├── 📁 backend/          → Python API (FastAPI)
├── 📁 frontend/         → React приложение
├── 📁 .github/          → Автодеплой через GitHub Actions
├── 📄 README.md         → Полная документация
├── 📄 SERVER_SETUP.md   → Настройка сервера
├── 📄 USER_GUIDE.md     → Руководство пользователя
├── 📄 docker-compose.yml → Запуск всего приложения
└── 📄 .env.example      → Шаблон конфигурации
```

## ⚡ Быстрый локальный запуск

```bash
# 1. Распакуйте архив
tar -xzf barber-crm-complete.tar.gz
cd barber-crm

# 2. Создайте .env файл
cp .env.example .env
nano .env  # Вставьте свои данные

# 3. Запустите
docker-compose up -d

# 4. Откройте в браузере
# http://localhost
```

## 🔑 Что нужно вставить в .env?

1. **JWT_SECRET** - любая длинная строка (минимум 32 символа)
2. **GOOGLE_SHEET_ID** - из URL таблицы
3. **GOOGLE_SERVICE_ACCOUNT_JSON** - содержимое JSON файла

## 🌐 Деплой на сервер через GitHub

### Шаг 1: Настройте сервер
```bash
ssh root@ваш_сервер
apt update && apt install -y docker.io docker-compose git
mkdir -p /opt/barber-crm
```

### Шаг 2: Загрузите код на GitHub
```bash
cd barber-crm
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/barber-crm.git
git push -u origin main
```

### Шаг 3: Настройте Secrets в GitHub
Settings → Secrets → Actions → New secret:

- `JWT_SECRET` = ваш секретный ключ
- `GOOGLE_SHEET_ID` = ID таблицы
- `GOOGLE_SERVICE_ACCOUNT_JSON` = JSON ключ
- `SERVER_HOST` = IP сервера
- `SERVER_USER` = root
- `SSH_PRIVATE_KEY` = SSH ключ для доступа

### Шаг 4: Push = автодеплой!
```bash
git add .
git commit -m "Update"
git push
# → GitHub Actions автоматически задеплоит на сервер!
```
111111
## 📊 Где данные?

Все данные автоматически сохраняются в Google Sheets в отдельных листах для каждого филиала:

- `Утренние мероприятия - Станкевича`
- `Полевые выходы - Станкевича`
- `One-on-One - Станкевича`
- и т.д.

**Новые записи всегда сверху!** ⬆️

## 🎨 8 вкладок приложения

1. 🌅 Утренние мероприятия
2. 🏃 Полевые выходы
3. 👥 One-on-One встречи
4. 📊 Еженедельные показатели
5. 🎓 Адаптация новичков
6. 🎯 Планы мастеров
7. ⭐ Отзывы
8. 📈 Сводка филиала

## 🛠️ Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Полная пересборка
docker-compose up -d --build
```

## ❓ Проблемы?

**Не запускается:**
```bash
docker-compose down
docker system prune -af
docker-compose up -d --build
```

**Данные не попадают в таблицу:**
1. Проверьте Service Account email в доступах к таблице
2. Проверьте GOOGLE_SHEET_ID в .env
3. Посмотрите логи: `docker-compose logs backend`

**Автодеплой не работает:**
1. Проверьте все Secrets в GitHub
2. Проверьте SSH доступ к серверу
3. Посмотрите логи Actions в GitHub

## 📚 Документация

- **README.md** - полная документация проекта
- **SERVER_SETUP.md** - детальная настройка сервера
- **USER_GUIDE.md** - руководство для пользователей

## 💡 Добавить филиал

```python
# backend/main.py
BRANCHES = [
    "Станкевича",
    "Центральный",
    "НОВЫЙ_ФИЛИАЛ"  # ← добавьте здесь
]
```

```bash
docker-compose up -d --build
```

## 🎯 Готово!

Приложение работает! Теперь франшизники могут:
- Заходить по общей ссылке
- Выбирать свой филиал
- Заполнять данные
- Видеть их в Google Sheets

**Удачи в управлении филиалами! 💈✨**
