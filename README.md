# BarberCRM - Система управления филиалами барбершопов

Современная CRM-система для управления франшизными филиалами барбершопов с автоматической синхронизацией с Google Sheets.

## 🎯 Возможности

### 8 вкладок для комплексного управления:

1. **Утренние мероприятия** - учет запусков дня, срезов, допов и улучшений
2. **Полевые выходы** - оценка качества работы мастеров (стрижки, сервис, продажи)
3. **One-on-One встречи** - индивидуальные встречи с сотрудниками
4. **Еженедельные показатели** - планы и факты по основным метрикам филиала
5. **Адаптация новичков** - чек-лист онбординга новых барберов
6. **Планы мастеров** - индивидуальные месячные планы и их выполнение
7. **Отзывы** - учет и контроль отзывов
8. **Сводка филиала** - общая аналитика и контроль KPI

### Ключевые особенности:

✅ **Авторизация по филиалам** - каждый филиал работает со своими данными  
✅ **Автоматическая синхронизация** - все данные моментально попадают в Google Sheets  
✅ **Новые записи сверху** - удобная сортировка (последние записи всегда наверху)  
✅ **Множественное добавление** - можно добавить до 8-10 записей за раз  
✅ **Автоматические расчеты** - проценты выполнения, общие оценки и метрики  
✅ **Современный интерфейс** - адаптивный дизайн для мобильных и десктопов  
✅ **Автодеплой через GitHub** - push в main → автоматическое обновление на сервере

## 🚀 Быстрый старт

### 1. Подготовка Google Sheets

1. Создайте новую Google таблицу
2. Скопируйте ID таблицы из URL: `https://docs.google.com/spreadsheets/d/ВАШ_ID/edit`
3. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
4. Создайте новый проект
5. Включите Google Sheets API
6. Создайте Service Account и скачайте JSON ключ
7. Предоставьте доступ к таблице email из Service Account JSON

### 2. Настройка репозитория

```bash
# Клонируйте этот репозиторий
git clone https://github.com/ваш-username/barber-crm.git
cd barber-crm

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env и вставьте ваши данные:
# - JWT_SECRET (любая длинная строка)
# - GOOGLE_SHEET_ID (ID вашей таблицы)
# - GOOGLE_SERVICE_ACCOUNT_JSON (содержимое JSON файла)
```

### 3. Локальный запуск

```bash
# Запуск через Docker
docker-compose up -d

# Приложение будет доступно:
# Frontend: http://localhost
# Backend API: http://localhost:8000
```

### 4. Настройка GitHub Actions для автодеплоя

Добавьте следующие секреты в настройках GitHub репозитория (Settings → Secrets and variables → Actions):

- `JWT_SECRET` - ваш секретный ключ
- `GOOGLE_SHEET_ID` - ID Google таблицы
- `GOOGLE_SERVICE_ACCOUNT_JSON` - содержимое JSON файла (целиком)
- `SERVER_HOST` - IP адрес вашего сервера
- `SERVER_USER` - username для SSH (обычно `root` или `ubuntu`)
- `SSH_PRIVATE_KEY` - приватный SSH ключ для доступа к серверу

После настройки секретов каждый push в ветку `main` будет автоматически деплоить приложение на сервер!

## 📁 Структура проекта

```
barber-crm/
├── backend/
│   ├── main.py              # FastAPI приложение с API
│   ├── requirements.txt     # Python зависимости
│   └── Dockerfile
├── frontend/
│   ├── App.jsx             # React приложение
│   ├── index.html
│   ├── package.json
│   └── Dockerfile
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions для автодеплоя
├── docker-compose.yml
├── .env                    # Переменные окружения (не в git!)
└── README.md
```

## 🔧 API Endpoints

### Авторизация
- `GET /branches` - список филиалов
- `POST /login` - авторизация по филиалу

### Данные
- `POST /morning-events` - утренние мероприятия
- `POST /field-visits` - полевые выходы
- `POST /one-on-one` - one-on-one встречи
- `POST /weekly-metrics` - еженедельные показатели
- `POST /newbie-adaptation` - адаптация новичков
- `POST /master-plans` - планы мастеров
- `POST /reviews` - отзывы
- `POST /branch-summary` - сводка по филиалу

## 📊 Структура данных в Google Sheets

Для каждого филиала создаются отдельные листы:

- `Утренние мероприятия - [Филиал]`
- `Полевые выходы - [Филиал]`
- `One-on-One - [Филиал]`
- `Еженедельные показатели - [Филиал]`
- `Адаптация новичков - [Филиал]`
- `Планы мастеров - [Филиал]`
- `Отзывы - [Филиал]`
- `Сводка - [Филиал]`

**Важно:** Новые записи всегда добавляются во вторую строку (после заголовков), сдвигая старые вниз. Это позволяет видеть последние данные сверху списка.

## 🎨 Особенности интерфейса

- **Адаптивный дизайн** - работает на телефонах, планшетах и компьютерах
- **Звездная система оценок** - наглядная оценка от 1 до 10
- **Автоматические расчеты** - все проценты и средние значения считаются на лету
- **Цветовое кодирование** - разные цвета для разных типов метрик
- **Множественные записи** - добавляйте несколько мастеров/событий за раз

## 🔐 Безопасность

- JWT токены для авторизации
- Переменные окружения для чувствительных данных
- CORS настроен для безопасности
- Service Account для доступа к Google Sheets

## 📝 Добавление нового филиала

Откройте `backend/main.py` и добавьте филиал в список:

```python
BRANCHES = [
    "Станкевича",
    "Центральный",
    "Восточный",
    "Западный",
    "НОВЫЙ ФИЛИАЛ"  # Добавьте здесь
]
```

Пересоберите контейнеры:
```bash
docker-compose up -d --build
```

## 🐛 Troubleshooting

### Ошибка подключения к Google Sheets
- Проверьте, что Service Account email имеет доступ к таблице
- Проверьте правильность GOOGLE_SHEET_ID
- Убедитесь, что Google Sheets API включен в проекте

### Автодеплой не работает
- Проверьте все секреты в GitHub
- Убедитесь, что SSH ключ корректный
- Проверьте доступность сервера на указанном хосте

### Данные не попадают в таблицу
- Откройте DevTools в браузере и проверьте Network вкладку
- Посмотрите логи backend: `docker-compose logs backend`
- Проверьте, что API_URL в App.jsx указывает на правильный адрес

## 📈 Roadmap

- [ ] Дашборд с графиками и аналитикой
- [ ] Экспорт данных в Excel
- [ ] Уведомления в Telegram
- [ ] Мобильное приложение
- [ ] Интеграция с iClients API

## 👥 Автор

Создано для управления сетью барбершопов

## 📄 Лицензия

MIT License - используйте свободно для своих проектов
