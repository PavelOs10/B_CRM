# BarberCRM v2.0 - Система управления филиалами барбершопов

Современная CRM-система для управления франшизными филиалами барбершопов с автоматической синхронизацией с Google Sheets.

## 🎯 Что нового в версии 2.0

### ✨ Основные изменения:

1. **Система регистрации филиалов** - больше не нужно хардкодить филиалы в коде
2. **Индивидуальные пароли** - каждый филиал регистрируется со своим паролем
3. **Улучшенная безопасность** - хеширование паролей SHA-256, токены для сессий
4. **Полная валидация данных** - проверка всех входящих данных на backend
5. **Детальное логирование** - все действия записываются в логи
6. **Обработка ошибок** - понятные сообщения об ошибках для пользователя
7. **Health checks** - автоматическая проверка работоспособности сервисов
8. **Сохранение сессии** - не нужно входить заново после обновления страницы

## 🚀 Быстрый старт

### 1. Подготовка Google Sheets

1. Создайте новую Google таблицу
2. Скопируйте ID таблицы из URL: `https://docs.google.com/spreadsheets/d/ВАШ_ID/edit`
3. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
4. Создайте новый проект
5. Включите **Google Sheets API**
6. Создайте **Service Account** и скачайте JSON ключ
7. Предоставьте доступ к таблице email из Service Account JSON (например: `your-service@project.iam.gserviceaccount.com`)

### 2. Настройка репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/ваш-username/barber-crm.git
cd barber-crm

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env и вставьте данные:
nano .env
```

Вставьте в `.env`:
```bash
GOOGLE_SHEET_ID=ваш_sheet_id
GOOGLE_SERVICE_ACCOUNT_JSON='{"type": "service_account", "project_id": "..."}'
```

### 3. Локальный запуск

```bash
# Запуск через Docker Compose
docker compose up -d

# Проверка логов
docker compose logs -f

# Приложение будет доступно:
# Frontend: http://localhost
# Backend API: http://localhost:8000
# API документация: http://localhost:8000/docs
```

### 4. Настройка GitHub Actions для автодеплоя

Добавьте следующие секреты в GitHub (Settings → Secrets and variables → Actions):

- `GOOGLE_SHEET_ID` - ID Google таблицы
- `GOOGLE_SERVICE_ACCOUNT_JSON` - содержимое JSON файла (целиком)
- `SERVER_HOST` - IP адрес вашего сервера
- `SERVER_USER` - username для SSH (обычно `root` или `ubuntu`)
- `SSH_PASSWORD` - пароль для SSH доступа к серверу

## 📖 Как использовать

### Регистрация нового филиала

1. Откройте приложение
2. Нажмите кнопку **"Регистрация"**
3. Заполните форму:
   - Название филиала (например: "Станкевича")
   - Адрес (например: "ул. Станкевича, 23")
   - Имя управляющего
   - Телефон
   - Пароль (минимум 6 символов)
   - Подтверждение пароля
4. Нажмите **"Зарегистрировать филиал"**
5. После успешной регистрации автоматически переключится на форму входа

### Вход в систему

1. Введите название филиала (точно как при регистрации)
2. Введите пароль
3. Нажмите **"Войти"**

### Работа с данными

После входа вам доступны разделы:
- **Утренние мероприятия** - `/morning-events/{название_филиала}`
- **Полевые выходы** - `/field-visits/{название_филиала}`
- **One-on-One встречи** - `/one-on-one/{название_филиала}`
- **Еженедельные показатели** - `/weekly-metrics/{название_филиала}`
- **Адаптация новичков** - `/newbie-adaptation/{название_филиала}`
- **Планы мастеров** - `/master-plans/{название_филиала}`
- **Отзывы** - `/reviews/{название_филиала}`
- **Сводка по филиалу** - `/branch-summary/{название_филиала}`

## 📊 Структура данных в Google Sheets

### Лист "Филиалы"
Создается автоматически при первой регистрации. Содержит:
- Дата регистрации
- Название
- Адрес
- Управляющий
- Телефон
- Пароль (хеш)
- Токен
- Статус

### Листы для каждого филиала
Создаются автоматически при добавлении данных:
- `Утренние мероприятия - [Название]`
- `Полевые выходы - [Название]`
- `One-on-One - [Название]`
- `Еженедельные показатели - [Название]`
- `Адаптация новичков - [Название]`
- `Планы мастеров - [Название]`
- `Отзывы - [Название]`
- `Сводка - [Название]`

## 🔐 Безопасность

- ✅ Пароли хешируются SHA-256
- ✅ Токены генерируются cryptographically secure
- ✅ Валидация всех входных данных
- ✅ CORS настроен корректно
- ✅ Service Account для доступа к Google Sheets
- ✅ Логирование всех действий

## 🛠 API Endpoints

### Авторизация
- `POST /register` - регистрация нового филиала
- `POST /login` - вход в систему

### Проверка здоровья
- `GET /` - базовый health check
- `GET /health` - детальная проверка

### Данные (требуют авторизации)
- `POST /morning-events/{branch_name}` - добавить утренние мероприятия
- `GET /morning-events/{branch_name}` - получить утренние мероприятия
- `POST /field-visits/{branch_name}` - добавить полевые выходы
- `POST /one-on-one/{branch_name}` - добавить One-on-One встречи
- `POST /weekly-metrics/{branch_name}` - добавить еженедельные показатели
- `POST /newbie-adaptation/{branch_name}` - добавить адаптацию новичков
- `POST /master-plans/{branch_name}` - добавить планы мастеров
- `POST /reviews/{branch_name}` - добавить отзывы
- `POST /branch-summary/{branch_name}` - добавить сводку

## 🐛 Troubleshooting

### Ошибка "Google Sheets не настроен"
- Проверьте, что `GOOGLE_SERVICE_ACCOUNT_JSON` установлен в `.env`
- Убедитесь, что JSON валидный (можно проверить на jsonlint.com)

### Ошибка "Не удалось подключиться к Google Sheets"
- Проверьте, что Service Account email имеет доступ к таблице
- Проверьте правильность `GOOGLE_SHEET_ID`
- Убедитесь, что Google Sheets API включен в проекте

### Ошибка "Филиал с таким названием уже существует"
- Выберите другое название филиала
- Или используйте существующий филиал для входа

### Не могу войти
- Проверьте правильность написания названия филиала (учитывается регистр)
- Убедитесь, что пароль введен правильно
- Посмотрите в Google Sheets лист "Филиалы" - там должна быть запись

### Данные не сохраняются
- Откройте DevTools (F12) → Network
- Посмотрите логи backend: `docker compose logs backend`
- Проверьте, что в Google Sheets созданы нужные листы

### Проблемы с Docker
```bash
# Пересоздать контейнеры
docker compose down
docker compose up -d --build

# Посмотреть логи
docker compose logs -f

# Проверить статус
docker compose ps
```

## 📈 Отличия от версии 1.0

| Функция | v1.0 | v2.0 |
|---------|------|------|
| Филиалы | Хардкод в коде | Регистрация через UI |
| Пароль | Один для всех | Индивидуальный для каждого |
| Валидация | Минимальная | Полная на backend |
| Ошибки | Generic | Детальные сообщения |
| Логи | Отсутствуют | Подробное логирование |
| Сессии | Нет | Сохранение в localStorage |
| Health checks | Нет | Есть для обоих сервисов |

## 🎨 Особенности интерфейса

- ✅ Адаптивный дизайн (телефоны, планшеты, десктопы)
- ✅ Переключение между входом и регистрацией
- ✅ Валидация форм в реальном времени
- ✅ Понятные сообщения об ошибках
- ✅ Автоматическое сохранение сессии
- ✅ Подтверждение перед выходом

## 📝 Рекомендации

1. **Безопасность**:
   - Используйте сложные пароли (минимум 8 символов, буквы + цифры + спецсимволы)
   - Не делитесь паролями между филиалами
   - Регулярно меняйте пароли

2. **Google Sheets**:
   - Не редактируйте вручную листы с данными
   - Делайте резервные копии таблицы
   - Ограничьте доступ к таблице только необходимым пользователям

3. **Сервер**:
   - Используйте HTTPS в продакшене
   - Настройте firewall
   - Регулярно обновляйте систему

## 🔄 Обновление

```bash
# На сервере
cd /opt/barber-crm-app
git pull origin main
docker compose down
docker compose up -d --build
```

Или просто сделайте push в main - GitHub Actions автоматически обновит приложение!

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте health check: `curl http://localhost:8000/health`
3. Посмотрите в Google Sheets - создаются ли листы
4. Проверьте Network в DevTools браузера

## 📄 Лицензия

MIT License - используйте свободно для своих проектов

---

**Версия:** 2.0.0  
**Дата:** Февраль 2026  
**Автор:** Создано для управления сетью барбершопов